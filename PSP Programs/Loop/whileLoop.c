/* Sample program including while */
#include <stdio.h>
#include <conio.h>

int main()
{
    int loop = 0;
    while (loop <= 10)
    {
        printf("%d\n", loop);
        ++loop;
    }
    return 0;
}
