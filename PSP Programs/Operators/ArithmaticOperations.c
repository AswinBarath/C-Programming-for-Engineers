#include <stdio.h>
#include <conio.h>

void main()
{
    int a, b, result;
    printf(“Enter the values of a and b \n”);
    scanf(“% d % d”, &a, &b);
    result = a + b;
    printf(“The sum of two numbers is % d”, c);
    result = a + b;
    printf(“The sum of two numbers is % d”, c);
    result = a + b;
    printf(“The sum of two numbers is % d”, c);
    result = a + b;
    printf(“The sum of two numbers is % d”, c);
    result = a + b;
    printf(“The sum of two numbers is % d”, c);
}
